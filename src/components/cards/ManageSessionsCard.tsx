import React from "react";
import {Card, CardContent} from "@mui/material";

export const ManageSessionsCard = () => {
  return (
      <Card>
          <CardContent>

          </CardContent>
      </Card>
  )
};