import {SessionManagementPage} from "../../pages/SessionManagementPage";

export const ROUTES = {
    sessionManagement: {
        route: '/sessions',
        component: SessionManagementPage,
    }
}